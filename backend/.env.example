# ═══════════════════════════════════════════════════════════════
# SAFEGUARD FAMILY - BACKEND CONFIGURATION
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values
# NEVER commit .env to version control!
# ═══════════════════════════════════════════════════════════════

# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MongoDB)
# Option 1: Local MongoDB
MONGODB_URI=mongodb://localhost:27017/safeguard-family

# Option 2: MongoDB Atlas (Cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/safeguard-family?retryWrites=true&w=majority

# JWT Secret (Change this to a random string!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Email Configuration (Gmail SMTP)
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password
# Note: For Gmail, you need to generate an "App Password" from Google Account settings
# Go to: https://myaccount.google.com/apppasswords

# ESP32 Configuration
ESP32_ENABLED=true
ESP32_ALERT_URL=http://192.168.1.100/alert
# Change this to your ESP32's IP address on your local network

# URL Classification Settings
ENABLE_AI_CLASSIFICATION=false
# Set to true if you want to use AI-based classification (requires API key)

# OpenAI API (Optional - for advanced URL classification)
# OPENAI_API_KEY=your-openai-api-key

# Data Retention (days)
DATA_RETENTION_DAYS=30

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000
EXTENSION_ID=chrome-extension://your-extension-id

# Security
BCRYPT_ROUNDS=10

# Logging
LOG_LEVEL=info
